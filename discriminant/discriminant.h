#ifndef DISCRIMINANT_H_INCLUDED   //preprocessor directives to avoid including this header file twice on compilation
#define DISCRIMINANT_H_INCLUDED

double discriminant(float a, float b, float c );

#endif
